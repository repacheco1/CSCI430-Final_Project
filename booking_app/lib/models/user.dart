//class name is AppUser because firebase auth
//has object User to avoid conflicts
class AppUser {
  final String uid; // getting user ID

  // ignore: sort_constructors_first
  AppUser({this.uid});
}
